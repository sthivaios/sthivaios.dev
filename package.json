{
  "name": "sthivaios.dev",
  "private": true,
  "version": "1.0.0",
  "description": "sthivaios.dev",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --parallel build"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "prettier": "^3.8.3"
  },
  "dependencies": {
    "next-themes": "^0.4.6"
  }
}